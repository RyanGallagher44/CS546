<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>{{title}}</title>
        <link rel="stylesheet" href="/public/main_styles.css">
    </head>
    <body>
        {{#if header}}
        <h1>{{header}}</h1>
        {{/if}}
        <main>
            {{{body}}}
        </main>
    </body>
</html>